{% set name = "nglview" %}
{% set version = "1.2.0" %}
{% set sha256 = "19956518e355826e07a6a0b1553ce9b81d6825e52a399a51adf0a8c5f9965b25" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: |
     python -m pip install --no-deps --ignore-installed --install-option="--conda" . ;
     jupyter-nbextension install nglview --py --sys-prefix ;
     jupyter-nbextension enable nglview --py --sys-prefix
  noarch: python

requirements:
  build:
    - python
    - pip
    - numpy
    - ipywidgets >=7
    - notebook
  run:
    - python
    - numpy
    - ipywidgets >=7
    - notebook

test:
  imports:
    - nglview 

about:
  home: http://github.com/arose/nglview
  license: MIT
  license_file: LICENSE
  summary: 'An IPython widget to interactively view molecular structures and trajectories. Utilizes the embeddable NGL Viewer for rendering.'
  doc_url: https://arose.github.io/nglview/latest/ 
  dev_url: https://github.com/arose/nglview

extra:
  recipe-maintainers:
    - marscher
    - gph82
    - hainm
 
